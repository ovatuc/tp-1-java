//8.- Escribe un programa que pida un número y diga si es divisible por 2

let numero = parseInt(prompt('Escriba un numero '));

if (numero / 2) {
  document.write('El numero si es divisible en 2: ' + numero);
}else {
  document.write('El numero no es divisible en 2: ' + numero);
}